import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Leo Real Estate Assistant",
  description:
    "I’m here to help you with all your real estate needs. Whether it’s property listings, neighborhood insights, or even advice on financing and legalities, I’ll provide tailored suggestions and guide you to the best choices. Let’s get started",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased MainBG`}
      >
        {children}
      </body>
    </html>
  );
}
